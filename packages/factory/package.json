{
  "name": "@typescript-runtime-schema/factory",
  "version": "1.0.0",
  "description": "A wrapper for the typescript factory that provide sensible currying to the API",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest src/",
    "build": "yarn build:ts",
    "build:ts": "./node_modules/.bin/tsc --project tsconfig.json",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.5.3",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
