{
  "name": "typescript-runtime-schema",
  "description": "Mono repository for packages related to the runtime schema typescript transformer.",
  "repository": "git@github.com:simonlovesyou/typescript-schema.git",
  "author": "Simon Johansson <simonljohansson92@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "scaffold": "node ./.bin/scaffold",
    "bootstrap": "lerna bootstrap",
    "clean": "find . -type d -name node_modules -exec rm -rf {} \\;",
    "build:docs": "./node_modules/.bin/generate-docs --index-file-path ./docs/index.md -o README.md"
  },
  "devDependencies": {
    "@typescript-runtime-schema/generate-docs": "file:./packages/generate-docs",
    "lerna": "^3.22.1",
    "unorepo": "^0.1.0"
  }
}
